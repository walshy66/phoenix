# Scope Lock — COA-74 Resource Filters Mobile UX

- Feature branch: `cameronwalsh/coa-74-resource-filters`
- Locked scope: mobile filter state persistence, mobile filter panel UX, horizontal resource tab carousel, auto-collapse on scroll
- Out of scope: server-side persistence, new dependencies, search autocomplete, analytics dashboard
- Validation completed with `npm test` and `npm run build`
