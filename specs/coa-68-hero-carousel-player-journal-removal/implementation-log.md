# Implementation Log — COA-68

## Window 1
- Added a regression test to ensure `player_journal.png` is not present in the hero slides.
- Removed the `player_journal` slide from `src/pages/index.astro`.
