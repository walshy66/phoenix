# Scope Lock — COA-27

In scope:
1. Modal display for `video`, `pdf`, `document` resources on `/resources`
2. Placeholder and link behavior updates on resource cards
3. `sourceDomain` support in types and card rendering
4. Merge CLI for approved candidate resources
5. Scrape CLI for one-time candidate generation

Out of scope:
- Backend/database
- Runtime scraping during build/deploy
- Deep-linking modal URL state
