# Scope Lock: COA-93 — Scores Refresh

## In Scope
- Update `.github/workflows/playhq-sync.yml` refresh cadence
- Add Melbourne local-time gating so scheduled runs only proceed inside the evening window
- Keep existing data-refresh scripts and deployment flow intact

## Out of Scope
- UI changes
- New data pipeline scripts
- Linear status updates beyond implementation tracking
