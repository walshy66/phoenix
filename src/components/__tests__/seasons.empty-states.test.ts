import { describe, test, expect } from 'vitest'

describe('seasons empty-state guardrails', () => {
  test('placeholder suite replaced by data-driven cards', () => {
    expect(true).toBe(true)
  })
})
